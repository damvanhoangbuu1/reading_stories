<div class="row">
  <% @chapters.each do |c| %>
      <div class="col-xs-4 col-sm-4 col-md-4 col-lg-4">
      <%= link_to "Chapter #{c.chapter_number}: #{c.name}", chapter_path(c), style:"text-decoration: none;" %><p>
      </div>
  <% end %>
  </div>
</div>